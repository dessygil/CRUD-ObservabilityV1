# CRUD-ObservabilityV1
CRUD app written in go


needs to have CI/CD testing both with jest and testify
- built everything and pushed to docker hub and used minikube and kubectl to then take everything and run it on kubernetes but it requires me to have two terminals open to 